+++
title = "Migration"
description = "Coming from another index server or cache? One page per tool: what changes, what you gain, what you give up."
sort_by = "weight"
template = "section.html"
+++

Every tool here is good at the job it was built for. Each page maps one of them against velodex: the equivalents table
for what you run and configure today, the measured performance where the [benchmark suite](@/explanation/performance.md)
covers the tool, and the honest list of things velodex does not replace. If you want the decision matrix instead of the
tour, start with [when to use velodex](@/explanation/when-to-use.md).
