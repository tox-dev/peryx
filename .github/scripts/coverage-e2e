#!/usr/bin/env bash
set -euo pipefail

repo="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/../.." && pwd)"
cd "$repo"
cargo llvm-cov nextest -p peryx --features e2e --lcov --output-path "${1:-coverage-e2e.lcov}"
