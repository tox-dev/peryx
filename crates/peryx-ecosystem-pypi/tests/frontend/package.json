{
  "name": "peryx-pypi-frontend-tests",
  "private": true,
  "scripts": {
    "test": "playwright test --config playwright.config.mjs"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0"
  }
}
