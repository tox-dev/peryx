{
  "name": "peryx-oci-frontend-tests",
  "private": true,
  "scripts": {
    "test": "playwright test --config playwright.config.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
