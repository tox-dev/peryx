# Vendored third-party artwork stays byte-for-byte as published.
exclude: ^site/static/logos/

ci:
  autoupdate_schedule: monthly
  # pre-commit.ci has no Rust toolchain; CI runs fmt/clippy instead
  skip: [cargo-fmt, cargo-clippy]

repos:
  - repo: local
    hooks:
      - id: cargo-fmt
        name: cargo fmt
        language: system
        entry: cargo fmt --all --check --
        types: [rust]
        pass_filenames: false
      - id: cargo-clippy
        name: cargo clippy
        language: system
        entry: cargo clippy --workspace --all-targets -- -D warnings
        types: [rust]
        pass_filenames: false
  - repo: https://github.com/hukkin/mdformat
    rev: 1.0.0
    hooks:
      - id: mdformat
        additional_dependencies:
          - mdformat-gfm>=1
          - mdformat-gfm-alerts>=2
          - mdformat-footnote>=0.1.3
          - mdformat-deflist>=0.1.4
          - mdformat-simple-breaks>=0.1
          - mdformat-toc>=0.5
          - mdformat-config>=0.2.1
          - mdformat-web>=0.2
          - mdformat-rustfmt>=0.0.3
          - mdformat-zola>=1
        args: ["--wrap", "120"]
  - repo: https://github.com/python-jsonschema/check-jsonschema
    rev: 0.37.4
    hooks:
      - id: check-github-workflows
        args: ["--verbose"]
  - repo: https://github.com/codespell-project/codespell
    rev: v2.4.2
    hooks:
      - id: codespell
        args: ["--ignore-words-list=ser"]
        # Generated mermaid SVGs contain mermaid's own class names (e.g. `.composit`).
        exclude: "^site/diagrams/"
  - repo: https://github.com/zizmorcore/zizmor-pre-commit
    rev: v1.26.1
    hooks:
      - id: zizmor
  - repo: meta
    hooks:
      - id: check-hooks-apply
      - id: check-useless-excludes
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: end-of-file-fixer
      - id: trailing-whitespace
